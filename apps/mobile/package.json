{
  "name": "watch-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for Watch — loads the deployed web app in remote-URL mode (see PROJECT_KNOWLEDGE.md and the Phone App Roadmap artifact for the full plan).",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
